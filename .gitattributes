# ============================================================
# KcosGame · 换行符与二进制属性
# 目的：保证「发布副本 = 上游字节」这条约定不被 Git 的换行符
#       规范化悄悄破坏（本机 core.autocrlf=true）。
#
# 注意：gitattributes 是「最后匹配的规则生效」，所以这里的顺序
#       不能随意调整 —— 通用扩展名规则在前，game-*/ 例外在后。
# ============================================================

# ---------- 1. 门户、脚本与文档：统一 LF 入库 ----------
*.html           text eol=lf
*.mjs            text eol=lf
*.md             text eol=lf
.gitignore       text eol=lf
.gitattributes   text eol=lf

# ---------- 2. 二进制资源 ----------
*.png            binary

# ---------- 3. 游戏发布副本：逐字节发布，禁止任何换行符转换 ----------
# 必须放在 *.html 之后才能覆盖它。三者实际换行符并不一致：
#   game-maze/     上游是 CRLF
#   game-parkour/  上游是 LF
#   game-tenbase/  编译产物，含内联字体与脚本
# -text 让 Git 原样存储、原样检出，克隆到任何平台字节都不变。
# binary 同时关闭 diff，避免把整份压缩脚本当文本比对。
game-maze/**     -text binary
game-parkour/**  -text binary
game-tenbase/**  -text binary
